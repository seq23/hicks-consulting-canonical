import { buildProviderHealth } from './provider_truth.mjs';const h=buildProviderHealth(process.env,new Date());const required=['openrouter_web_search','gsc','bing'];const disconnected=required.filter(k=>['NOT_CONFIGURED','DISCONNECTED','DEGRADED'].includes(h.providers[k]?.state));console.log(JSON.stringify({...h,requiredProviderIssues:disconnected},null,2));if(process.env.REQUIRE_LIVE_SEARCH_PROVIDERS==='1'&&disconnected.length)process.exit(2);
